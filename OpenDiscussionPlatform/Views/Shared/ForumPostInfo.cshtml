
<div class="panel-heading forumpost-heading">
    <h1 class="forumpost-title">@Model.Title</h1>
</div>

<div class="panel-body">
    <span class="forumpost-content-title">Continut postare</span>
    <br />
    <div class="forumpost-content-wrapper">
        @Html.Raw(Model.Content)
    </div>
</div>
<div class="forumpost-meta">
    <span class="">
        <i class="glyphicon glyphicon-time"></i>
        @Model.Date
    </span>
    <div>
        <i class="glyphicon glyphicon-bookmark"></i>
        @Model.Category.CategoryName
    </div>
    <div>
        <i class="glyphicon glyphicon-user"></i>
        <strong>
            @if (ViewBag.AfisareProfiluri)
            {
                <a href="/Users/Show/@Model.User.Id">  @Model.User.UserName </a>
            }
            else
            {
                @Model.User.UserName
            }
        </strong>
    </div>
</div>

